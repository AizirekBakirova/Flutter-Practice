import 'package:flutter/widgets.dart';

class AppClass extends StatelessWidget {
  const AppClass({super.key, required this.icon});
  final IconData icon;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
